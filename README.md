# Internship-Management-Database

# 🚀 Internship Management Database (NextStep)

Welcome to the **NextStep** database project! This repository hosts the `Internship.sql` script designed to create and populate a comprehensive database for managing internship workflows—connecting Students, Employers, and Internships seamlessly.

---

## 📖 Overview
This project provides a robust SQL Server database schema to handle: 
- **Users & Authentication**
- **Student & Employer Profiles**
- **Internship Listings**
- **Application Tracking**

It includes pre-loaded sample data, reporting queries for analytics, and transaction examples for status updates.

---

## 🛠️ Prerequisites

Before getting started, ensure you have the following Microsoft tools installed:

1.  **SQL Server Database Engine**  
    *Recommended:* [SQL Server Express](https://www.microsoft.com/en-us/sql-server/sql-server-downloads) (Free & Lightweight).
2.  **SQL Server Management Studio (SSMS)**  
    The graphical interface to manage your database. [Download SSMS here](https://learn.microsoft.com/en-us/sql/ssms/download-sql-server-management-studio-ssms).

---

## 📥 Installation Guide

### Step 1: Install SQL Server
1.  Download the **SQL Server Express** installer.
2.  Run the executable (e.g., `SQLServer2022-SSEI-Expr.exe`).
3.  Choose the **Basic** installation type.
4.  Follow the prompts to complete the setup.

### Step 2: Install SSMS
1.  Download the latest **SSMS** installer.
2.  Run the file (e.g., `SSMS-Setup-ENU.exe`).
3.  Click **Install** and restart your computer if prompted.

> 💡 **Tip:** Need help? Watch this quick 2-minute guide on [Installing SQL Server Management Studio](https://www.youtube.com/results?search_query=Install+SQL+Server+Management+Studio).

---

## ⚡ How to Run the Script

Once your tools are ready, follow these steps to deploy the database:

1.  **Launch SSMS**  
    Open *SQL Server Management Studio* from your Start Menu.

2.  **Connect to Server**  
    *   **Server Name:** Usually `localhost`, `.`, or `.\SQLEXPRESS`.
    *   **Authentication:** Select *Windows Authentication*.
    *   Click **Connect**.

3.  **Open the Script**  
    *   Go to `File > Open > File...` and select `Internship.sql`. 
    *   *Alternatively, open a **New Query** window and paste the script contents directly.*

4.  **Execute**  
    Click the **▶ Execute** button (or press `F5`).

### ✅ What Happens Next?
The script will automatically:
1.  Create the `NextStep` database.
2.  Build all required tables (`User`, `Employer`, `Internship`, etc.).
3.  Populate tables with dummy data.
4.  Run a series of **Query Reports** to display the data.
5.  Execute sample **Transactions** (e.g., shortlisting a student).

---

## 📊 Viewing Results

Check the **Results** pane at the bottom of SSMS to see:
- Successful command messages.
- Data tables for Users, Internships, Applications, etc.
- Summary reports generated by the script.

---
*Happy Coding! 🎓*
